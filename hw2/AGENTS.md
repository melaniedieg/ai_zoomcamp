# Project instructions

Follow `_docs/specs.md` and `openapi.yaml` when changing behavior. Keep all frontend HTTP calls in `frontend/src/api.js`. Run `uv run pytest` from `backend/` and `npm run build` from `frontend/` after code changes. Do not commit SQLite database files, virtual environments, or `node_modules`.
