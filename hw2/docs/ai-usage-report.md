# AI usage and review

The user selected the restaurant waitlist manager project. Codex drafted the spec, API contract, frontend, backend, and tests. The implementation was kept small enough to review against the acceptance criteria. The backend tests cover validation, queue order, status transitions, and SQLite persistence across app instances. The frontend build checks syntax and bundles the browser code. Deployment and multi-host live updates are outside this homework's scope.
